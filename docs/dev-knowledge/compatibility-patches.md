# 兼容与问题修复（bugfix/compat）台账

本台账记录恢复预期行为、修复回归或适配上游变化的改动。KubeJS、配置、hotai、CDC 和 mixin 都可能承载这类修复，但目录不决定分类；有意新增或调整玩家体验的改动应记入 `content-map.md`。

## 分类原则

| 改动目的 | 记录位置 | 说明 |
|---|---|---|
| 新增或有意调整玩法、平衡、NPC、剧情、任务、物品、机器或系统 | `content-map.md` | 内容改动（feat），记录玩家可见意图、实现位置和状态。 |
| 恢复预期行为、修复错误或回归、适配上游模组变化 | 本台账 | 兼容与问题修复（bugfix/compat），记录问题、补丁、验证和复核/移除条件。 |

按一个明确目的汇总一行，不要求每个配方、配置或 JSON 单独立项；同一目标新增文件时更新该行的路径与验证状态即可。

| 修复 | 类型 | 问题或根因 | 补丁位置 | 验证与跟踪 | 复核/移除条件 | 状态 |
|---|---|---|---|---|---|---|
| TACZ 1.1.8 创造页签补丁启动失败 | 模组兼容 | TACZ 从 `1.1.4-hotfix` 升级到 `1.1.8-hotfix` 后，`ModCreativeTabs` 字节码变化；旧 `MemoryDiff` 无法应用并在构造 `GunMod` 时抛出 `Not all byts consumed from byte[]`。 | `hotai/com/tacz/guns/init/ModCreativeTabs.badiff`：按 1.1.8 重建 13 个页签图标供应方法，继续指向 Create/Applied Armorer 枪械资产。 | 已用 HotAI 的 `MemoryDiff` 与 `DefaultSerialization` 生成，并反序列化重放后逐字节匹配目标 class；客户端已确认修复生效，`latest.log` 记录 `Patched class: com/tacz/guns/init/ModCreativeTabs`。 | TACZ 再次修改 `ModCreativeTabs`，或上游允许直接配置这些页签图标时，重新生成或移除补丁。 | 已运行时验证 |
| TACZ-addon 1.1.8-hotfix2 开镜崩溃 | 模组兼容 | TACZ-addon 的开镜镜头兼容层仍引用已从 Shoulder Surfing Reloaded 5.0.7 移除的 `ShoulderSurfingImpl` 与 `api/model/Perspective`，开镜时抛出 `NoClassDefFoundError`。 | `hotai/com/mafuyu404/taczaddon/compat/ShoulderSurfingCompatInner.badiff`：迁移到同语义的 `ShoulderSurfing` 和 `api/client/Perspective` API；目标 JAR 通过 `mods/taczaddon.pw.toml` 与 `packwiz-files/mods/taczaddon-1.20.1-1.1.8-hotfix2-for-new-soph.jar` 固定分发。 | 已以 HotAI `MemoryDiff`/`DefaultSerialization` 生成并反序列化重放校验；Packwiz 同步和载荷 SHA-256 一致；`latest.log` 已记录 `Patched class`，第三人称持枪开镜和松开瞄准回归成功。 | TACZ-addon 更新该辅助类，或 Shoulder Surfing 再次调整 `ShoulderSurfing` / `Perspective` API 时重新生成；上游修复后移除。 | 已运行时验证 |
| KubeJS build.26 食物属性回归 | 模组兼容 | build.26 新增的 `FoodBuilder.of` JS 回调转换路径可能从空 builder 构建 `foodProperties`，所以未在回调中重新写入的饥饿、饱和度、肉类标记、`alwaysEdible`、`fastToEat` 和其他效果都可能消失，并非只有被移除的效果受影响。 | `mods/kubejs.pw.toml` 按当前试验目标回退 KubeJS 到 `2001.6.5-build.24`；`kubejs/startup_scripts/modifier_item.js` 恢复更新前的直接 `foodProperties` 修改方式，同时保留后续实际的物品 ID、效果和属性调整。 | 已核对 build.24/build.26 JAR：build.24 仍包含 `FoodBuilder.of` 与 `FoodProperties` 类型包装器，因此 build.24 只是较高版本的回退试验，不能仅凭静态检查确认已避开回归；build.24 SHA-1 为 `95594c74234c653fe93554d21a3728c32a1eff5f`。`node --check`、目标文件 `git diff --check` 通过；完整重启后的食物营养和效果回归仍待执行；已提交上游 [KubeJS #1171](https://github.com/kube-mods/kubejs/issues/1171)。 | 若 build.24 重启后仍丢失原始属性，则回退到不含该类型包装器的版本或等待上游修复；若验证通过，再保留 build.24 并继续观察上游 issue。 | build.24 试验中，待重启回归 |
| 木卫二地下冰龙穴切入地下海 | 模组兼容 | IAF Dragon Fix `2.0.0` 只按结构起始群系允许冰龙穴，随后以约 18–21 格半径挖掘；中心通过木卫二冰原/山脊白名单后，龙穴仍可能落入或切进相邻地下海与渊海陷窟，产生悬空和大量掉落方块。 | `hotai/com/iafdragonfix/structure/DragonDenPiece.badiff`：在实际地下中心及周围 16 格做 3×3 群系采样，命中 `#createdelight:blocks_ice_dragon_caves` 时跳过；标签位于 `kubejs/data/createdelight/tags/worldgen/biome/blocks_ice_dragon_caves.json`。 | 目标 class SHA-256 为 `BF0FF1B1…CE2DA`，ASM 验证及反汇编检查通过；HotAI 已完成 `.class` 转存并记录 `Patched class`，客户端成功进入木卫二。首次 `BadiffCli diff` 文件因格式不兼容导致启动崩溃，已由运行时生成的 `MemoryDiff` 替换；第二次启动重放及新区块分布待验证。 | IAF Dragon Fix 升级、`DragonDenPiece.generateCave` 改写，或 Northstar/Alex's Caves 调整木卫二地下海群系 ID 时重新生成补丁与标签；补丁必须通过 HotAI `.class` 转存生成，不得使用 `BadiffCli diff`。 | 运行时转换已确认，待重放与世界生成回归 |
| Northstar 月尘陨石坑下落卡顿 | 模组兼容 | Northstar `0.6.4` 的 `CraterFeature` 可在洞穴薄壳上写入 `northstar:moon_sand` 并请求 post-processing，进入模拟范围后会批量生成低重力下落实体。 | `CDC-mod-src/.../mixin/northstar/CraterFeatureMixin.java`：仅在候选下落方块下方为空时跳过坑底写入；`build.gradle` 同步 Northstar 开发依赖到当前文件 `8486123`。 | CDC `2.2.16f` 构建通过，最终 refmap 已确认 `WorldGenLevel.setBlock` 生产映射；完整整合包的原生月球与月背新区块实体峰值、MSPT 回归待执行。 | Northstar 调整 `CraterFeature.placeColumnBelow`、其写入调用点或 `natural_moon_blocks` 标签时复核；上游修复同一逻辑后移除 CDC Mixin。 | 已构建并同步，待游戏内回归 |
| Northstar 0.6.4 星图全解锁指令 | 模组兼容 | Northstar 从 `0.6.1` 更新到 `0.6.4` 后将 `PlanetProperties.scienceWeightExp()` 重命名为 `scienceDecayExp()`，导致 `/cd_atlas unlock_all` 在重算科研值时抛出命令异常且不写回星图 NBT。 | `kubejs/server_scripts/Northstar/space_atlas_commands.js`：改用当前 `scienceDecayExp()` 访问器。 | 已用实际 `0.6.1`/`0.6.4` JAR 的 `javap` 核对 API；脚本热重载 343/343、0 错误；因命令树不会随脚本热重载重建，运行时回归需重启集成服务器后执行。 | Northstar 再次调整 `PlanetProperties` 科研衰减字段或 `AtlasPlanet.recalculateScience` 签名时复核。 | 已修复，待重启回归 |
| Lightman’s Currency 持久交易员数字 ID 失效 | 模组兼容 | KubeJS 将拍卖行和持久交易员硬编码为 `0`、`7`、`10`，但 Lightman’s Currency `2.3.0.5` 的 long ID 由各世界存档分配并保存，不是 `PersistentTraders.json` 数组下标；当前世界查找返回 `null`，分别触发 `tradeData` 读取和 `addTrade` 调用异常。 | `kubejs/server_scripts/Lightmans Currency/white_list.js` 改用 `TraderDataCache.getTrader(String)` 按 `technology_help_trade`、`resource_trader` 查询；`kubejs/startup_scripts/custom/order/system.js` 改用 `TraderDataCache.getAuctionHouse()`，两处增加空值保护。 | 已核对当前 `PersistentTraders.json` 字符串 ID，并用实际 JAR 字节码确认 `getTrader(String)`、`getAuctionHouse()` API；两个 JS 的 `node --check` 与 `git diff --check` 通过，需完整重启后完成任务解锁和订单拍卖回归。 | Lightman’s Currency 调整持久交易员缓存或拍卖行 API 时复核；不得重新依赖世界分配的数字 ID。 | 已修复，待重启回归 |
| 专用服务端切石配方下标错位 | 模组兼容 | Fast Recipe Search `1.20.1-26.1` 同时替换客户端与服务端 `RecipeManager`，并将匹配配方改按配方 ID 排序；原 `side = "client"` 使专用服务端仍按原版产物翻译键排序，而切石机点击包只发送列表下标，导致两端同一下标指向不同产物。 | `mods/fast-recipe-search.pw.toml` 改为 `side = "both"`；从 `kubejs/config/createdelight_pack_integrity.json` 移除可选缺失放行项，并重新生成 expected 清单。 | 已用实际 JAR 的 `ClientPacketListenerMixin`、`ServerResourcesMixin` 与 `RecipeDB` 字节码核对双端替换和排序逻辑；Packwiz 完整同步成功，仍需客户端与专用服务端完整重启后回归切石机。 | 上游若恢复原版排序，或切石机协议改为按配方 ID 选择时，可重新评估端侧；此前必须保证客户端与服务端加载相同版本。 | 已同步，待专用服务器回归 |
| FancyMenu 多人服务器玩家名单文字偏移 | 模组兼容 | FancyMenu `3.9.8` 的多人界面布局在原版服务器列表之后绘制旧版 3D 玩家实体；服务器玩家样本 Tooltip 的背景已绘制，但文字仍在共享 GUI 缓冲中，随后被玩家实体渲染以模型矩阵提交，造成文字偏移到 Tooltip 右下角。`0.5.0.2` 使用 FancyMenu `3.7.0`，未出现该渲染顺序回归。 | `hotai/de/keksuccino/fancymenu/customization/element/elements/playerentity/v1/PlayerEntityElement.badiff`：旧玩家实体元素渲染前调用 `GuiGraphics.flush()`。 | 已核对原版 `JoinMultiplayerScreen` / `ServerSelectionList$OnlineServerEntry` 的直接 Tooltip 调用链、FancyMenu `3.7.0`/`3.9.8` 字节码差异；badiff 输入/输出 SHA-1 分别为 `4ed0e6cc…e98e` / `e1d7c618…0ff8`，重新应用结果与目标 class SHA-256 一致，ASM 基础字节码校验通过。需完整重启客户端后悬浮服务器人数回归。 | FancyMenu 升级、移除旧 `fancymenu_customization_player_entity` 元素或上游在 3D 实体渲染前主动提交 GUI 缓冲时，重新生成或移除补丁。 | 已补丁，待重启回归 |
| 木卫二悚怖实体环境免疫 | 模组兼容 | `prevent_summon.js` 只放行悚怖实体在木卫二生成，但未同步加入 Northstar 的低温与缺氧生存标签，导致生成后持续受到星球环境伤害。 | `kubejs/server_scripts/Create Northstar/tag.js`：将八种 `iceandfire:dread_*` 实体统一加入 `northstar:can_survive_cold` 与 `northstar:doesnt_require_oxygen`。 | 已静态核对实体清单与木卫二生成白名单一致；标签需执行 `/reload` 后在游戏内复核。 | Ice and Fire 调整悚怖实体注册表，或 Northstar 更改环境免疫标签时复核。 | 已修复，待游戏内回归 |
| 虚拟卫星持久数据、远程区块与实体航点 | 自定义玩法修复 | 首版部署读取不存在的 `level.gameTime`，使裸 `NaN` 破坏 JSON；远程高度查询未先生成区块，只得到空区块高度，且 Rhino 无法消歧 `WorldBorder.isWithinBounds(BlockPos/AABB)`；只写 `SpaceAtlasContent` 而不放置航点时，Northstar 发射阶段还会在目的地坐标查询 `ROCKET_WAYPOINT` POI 并报“缺少火箭航点”。 | `kubejs/server_scripts/Northstar/virtual_satellite_navigation.js`：改用 `level.time` 并迁移非有限数值；采样前调用 `ServerLevel.getChunk`，边界检查使用双坐标重载；在地表上一格通过 `setBlockAndUpdate` 放置 `northstar:rocket_waypoint`，将同一坐标和非空方向写入星图，并清理首版低一格的旧目的地；临时 `[CD-SAT]` 聊天与信息日志已在验证后移除，仅保留真正的异常错误日志。 | 数据卡 `-6240,-2432` 解析到地表 `-6256,276,-2448`，实际航点和星图目的地均为 `-6256,277,-2448`；`execute if block` 与 `/locate poi northstar:rocket_waypoint` 均确认航点及 POI，脚本热重载 340/340、0 错误，用户确认火箭可使用。 | 重启持久化与其他星球仍需回归；若正式系统迁移到 CDC `SavedData`，删除 KubeJS 迁移代码。 | 主世界实际发射已验证 |
| 月球废弃空间站缺少实体航点 | 自定义玩法修复 | 轨道遥测扫描仪只把栈桥坐标写入星图，目标处仍是 `create:metal_girder`，Northstar 发射时无法在该精确位置找到 `ROCKET_WAYPOINT` POI。 | `kubejs/server_scripts/Northstar/lunar_farside.js`：统一计算栈桥目的地坐标，在首次建站后放置 `northstar:rocket_waypoint`；再次扫描已生成空间站时重新放置，可修复旧存档或被破坏的航点。 | 热重载 340/340、0 错误；玩家现有空间站目的地 `northstar:earth_orbit@-36768,95,-34446` 已确认存在 `northstar:rocket_waypoint`，`/locate poi` 在 0 格外命中，实际发射待用户回归。 | Northstar 若取消目的地 POI 校验，或空间站改用结构模板自带航点时复核。 | 航点与 POI 已验证 |
| LazyTick × Vintage Improvements 压缩机工作盆缓存 | 模组兼容 | LazyTick `2.4.9` 的 Basin 配方缓存不会感知 Vacuum Chamber 继承的配方上下文变化，导致压缩机偶发需移动或重设过滤器后才加工。 | `mods/CreateLazyTick.pw.toml` 更新至 `2.5.15`，使用上游 `VacuumChamberBasinBypassMixin`/`IBasinLazyTickBypass`；`mods/vintageimprovements.pw.toml` 更新至 `0.3.7.8`，撤回 VI 侧临时快照 Mixin。 | 已核对 LazyTick 发布 JAR 包含上游旁路类，VI 发布 JAR 不再包含 LazyTick/`BasinStateSnapshot` Mixin；运行时 JAR SHA-256 分别为 `2416ABA4…BA77AD`、`BF590658…DAE4E`。 | LazyTick 后续移除该上游兼容或 VI 不再继承 Basin 配方路径时重新复核；无需在 VI 侧恢复重复补丁。 | 已同步，待游戏内压缩机回归 |
| Frycooks Delight 油菜作物模型 | 资源兼容 | Frycooks Delight `1.0.1` 的 8 个油菜阶段仍引用 `farmersdelight:block/crop_cross`；Farmer's Delight `1.3.2` 将通用模板重命名为 `template_crop_cross`，造成紫黑缺失模型。 | `kubejs/assets/farmersdelight/models/block/crop_cross.json`：以旧路径提供与新模板等价的模型。 | 已由用户通过资源重载确认紫黑块消失；[#2007](https://github.com/Jasons-impart/Create-Delight-Remake/issues/2007)。 | Frycooks Delight 更新为引用 `farmersdelight:block/template_crop_cross` 或不再引用旧路径后，移除覆盖并回归油菜 8 个阶段。 | 生效中 |
| 节气覆雪模型 Z-fighting | 模组兼容 | 三份 KubeJS 覆写模型均在 `a5183677` 为修复覆雪贴图闪烁而新增；Ecliptic Seasons `0.13.8.1` 的 `CullTopFaceWithSnow` 已提供原生顶面剔除，覆写模型成为冗余补丁。 | `config/eclipticseasons-client.toml` 启用 `CullTopFaceWithSnow`；删除 `kubejs/assets/eclipticseasons/models/block/{overlay,overlay_2,overlay_tiny}.json`。 | 已核对模型首次引入提交与原生模型路径；需重启客户端后在冬季覆雪方块及远距离视角回归。 | 节气移除或修改顶面剔除逻辑时，重新评估是否恢复模型补丁。 | 待游戏内回归 |
| Sequenced Pattern Provider × Quality Food Fluids 序列组装返回物识别 | 模组兼容 | SPP `0.3.0` 与 QFF 同时在 `SequencedAssemblyRecipe.advance` 的可取消 `RETURN` 注入中替换返回值；同优先级下 QFF 可能先结束回调链，使 SPP 的任务 Token 未复制到带 `QualityFoodFluidsSequencedAssembly` 数据的半成品，主控完成一步后不再认领。 | `D:\learnmod\SequencedPatternProvider`：SPP Mixin 优先级提高到 `1100`，并为无 Token 返回物增加“仅唯一活跃任务可认领、认领后补回 Token、多候选等待并告警”的兼容路径。 | `0.3.1` 已离线构建；最终 JAR 已确认含 `priority=1100` 与无 Token 回退方法，SHA-256 `B3C0F2E4…FE7063`；仍需在同时加载 QFF 的齿轮盛宴环境回归单任务、多任务及带品质流体步骤。 | 若 SPP/QFF 改为非取消式组合钩子，或 Create 提供稳定的序列组装上下文扩展点，可移除优先级约束；无 Token 回退应保留到确认所有兼容机器均传播任务数据后再复核。 | 已修复，待游戏内回归 |
| CCK 收割双格果丛与作物品质 | 模组兼容 | CCK `1.5.0` 重置 Collector's Reap `1.5.5` 青柠/石榴时复用当前半格状态，造成上下两格 `HALF` 相同并整株消失；Festival Delicacies 圆茄和 Collector's Reap 果丛右键直接弹出物品，Cultural Delights 玉米上下段又使用不同方块，均绕过或错取 Quality Food `2.3.3` 上下文。 | `CDC-mod-src/.../CreateDelightCoreHarvesterMovementBehaviorExtensions.java` 分别恢复 `LOWER/UPPER`；新增 Collector's Reap、Festival Delicacies 品质 Mixin；玉米和果丛生成上段时从下段复制 `LevelData`；`QualityFoodMixin` 与 `kubejs/server_scripts/Quality Food/tags.js` 将玉米上下段统一回溯到下段品质及其下方土壤。 | `./gradlew.bat build --no-daemon` 通过；已核对 CCK/CR/Festival/Cultural 实际字节码、Mixin refmap、JAR 内容与 SHA-256 `FC8AA132…D75E7CC`；运行 JAR、Packwiz 载荷和元数据已一致，真实右键与收割机回归待重启。 | CCK 修正双格果丛状态恢复、Quality Food 原生覆盖这些右键掉落，或相关方块/调用描述符变化时复核并缩减补丁。 | 已部署，待游戏内回归 |
| CCK 切菜板机械手不消耗工具 | 模组兼容 | CCK `CuttingBoardDeployingRecipe` 调用 `toolNotConsumed()`，但继承普通 `ProcessingRecipe`；Create `6.0.8` 只对 `ItemApplicationRecipe.shouldKeepHeldItem()` 保留工具，因此堆叠工具仍 `shrink`、耐久工具仍 `hurtAndBreak`。 | `CDC-mod-src/.../mixin/create/BeltDeployerCallbacksMixin.java` 仅在该 CCK 配方执行时跳过 Create 对机械手手持工具的第二次 `shrink`、`hurtAndBreak` 与 crafting remainder 生成，不影响传送带原料扣除。 | 已核对 Create `BeltDeployerCallbacks.activate` 字节码中的两个 `shrink` 顺序、耐久分支与返还物分支；Gradle 构建和 refmap 映射通过，真实机械手回归待客户端重启。 | CCK 改为继承 `ItemApplicationRecipe`、Create 扩展保留工具判定接口，或 `activate` 消耗流程变化时移除/重写补丁。 | 已构建并同步，待游戏内回归 |
| 订单提交端口大规模包裹验收 | 自定义玩法修复 | 提交端口只有 6 个货物输入槽，且 `Order.convertPackageToItemHandler` 固定使用 81 个拆包栈槽；单堆上限 16 时，大订单会在进入 `Order.checkAllPackages()` 前受容量限制或被静默截断。 | `kubejs/server_scripts/mbd2/order_submission_port.js` 保留 6 槽 UI，改用机器 `customData` 持久缓存 32 个完整包裹并支持分批退回、处理中锁定和拆机掉落；`kubejs/startup_scripts/custom/order/system.js` 按实际包裹内容动态创建拆包传输槽。 | JS 语法、语言 JSON、目标 diff 检查通过；模拟验证 `732+672+360`、2560 件、4096 件、4608 件及少 1 件拒绝，32 包理论容量为 `32×9×16=4608` 件，`32+6` 包退回无重复；实际 `ItemStack` NBT 与 `ItemEntity` API 已核对 Forge `47.4.16`，需完整重启客户端后回归持久化、验收和拆机掉落。 | Create 更改包裹内部槽数/序列化格式、MBD2 更改 `customData` 或移除事件语义，或订单规模超过 32 包时复核。 | 已修复，待重启回归 |
| Crash Assistant JEXL 复选框中文乱码 | 模组兼容 | Crash Assistant `1.11.11` 用未指定字符集的 `new String(Files.readAllBytes(path))` 读取 UTF-8 `.jexl`；中文 Windows 的 CP936 默认字符集将“我已知悉，不要再提示了”解码为乱码。 | 两套 `config/**/crash_assistant/scripts/startup/{10,20}_modlist_changes_warning.jexl` 改为从 `LanguageProvider` 读取 ASCII 本地化键；两套 `zh_cn.json` 新增对应 UTF-8 文案。 | 两套 JSON 解析通过，脚本引用与本地化键均已核对，`git diff --check` 通过；需重启并触发 Java 版本或模组清单警告确认显示。 | Crash Assistant 修复脚本读取为 UTF-8 或更新 JAR 时，回归两类警告；上游修复后可评估是否保留此兼容写法。 | 已修复，待游戏内验证 |
| Squareful 覆盖 Create 车站图标 | 资源兼容 | Squareful `v3.15 for MC1.20.1` 覆盖 `assets/create/textures/gui/widgets.png`，使 Create `6.0.8` 的地图车站图标及悬停高亮区域透明；上游在 1.20.2+ 版本提供了对应修复贴图。 | `packwiz-files/resourcepacks/Squareful-Create_Delight_Remake.zip` 增加官方 `assets/create/textures/gui/widgets.png`，并在 ZIP 内声明其为依赖 Squareful 的非独立扩展；`resourcepacks/squareful-create-delight-remake.pw.toml` 更新 SHA-256。配套包必须在 Squareful 后加载。 | 已核对官方文件内容、`256×256` 尺寸、ZIP 条目、扩展许可说明和 Packwiz 哈希；问题跟踪：[issue #2122](https://github.com/Jasons-impart/Create-Delight-Remake/issues/2122)，来源：[Squareful 官方贴图](https://github.com/XeKr/Squareful/blob/main/src/mods/assets/create/textures/gui/widgets.png)。尚未完成客户端画面回归。 | Squareful 发布 1.20.1 正式修正版、Create 贴图布局变化或上游撤回扩展许可时，移除该覆盖并重新验证；不得修改或替代 Squareful 本体 ZIP。 | 已打包，待游戏内验证 |
| CCK 多余的小麦面团替代配方 | 配方兼容 | Create: Central Kitchen `1.5.0` 的内置 datapack 额外注册 `farmersdelight:wheat_dough_from_water_alternative`，允许一桶水和三份小麦面粉直接合成 3 个发酵蛋黄面团；该配方绕过了已移除的 Farmer's Delight `dough` 类型配方。 | `kubejs/server_scripts/ratatouille/wheat&oat&rice.js`：按完整配方 ID 精确移除该替代配方。 | 已从 CCK JAR 内置 datapack 核对配方 JSON，并确认当前运行日志中 JEI 可复制到该 ID；脚本改动和 `git diff --check` 待验证，完整重启后需确认配方不再出现在 JEI。 | CCK 移除或重命名该 datapack 配方、改用其他合成路径，或整合包重新启用该配方时复核并调整移除列表。 | 已修复，待游戏内验证 |
| OEI 合并物品未继承配方标签 | 模组兼容 | OEI 启用 `Deeper_Replace` 时只在运行时执行物品等价判断；tag Ingredient 会先展开 `#minecraft:fishes` 等真实标签成员，因而 `culturaldelights:squid` 无法匹配 `farmersdelight:cooking/stuffed_nautilus_shell`。 | `CDC-mod-src/.../mixin/Minecraft/OEITagMirrorMixin.java`：在物品 `TagLoader` 完成加载后，将 OEI 直接物品映射的目标加入来源标签；`#tag → target` 仅加入被明确指定的标签，并由原版嵌套标签自然向父级传播。 | CDC [#107](https://github.com/Jasons-impart/Create-Delight-Core/pull/107) 已合并；用户已在 Minecraft 中确认 squid 的镜像标签生效，且可用于制作 stuffed nautilus shell。 | OEI 原生将替换目标写入真实标签，或其重载映射/配置反射接口、`TagLoader` 目标方法发生变化时复核或移除。 | 已运行时验证 |
